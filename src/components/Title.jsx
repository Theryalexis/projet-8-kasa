import React from 'react';

const Title = () => {
    return (
        <div>
            <div className=''>{titre}</div>
            <div className=''>{paragraphe}</div>
        </div>
    );
};

export default Title;